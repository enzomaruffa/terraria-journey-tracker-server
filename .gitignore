__pycache__/
*.py[cod]
*.so
.Python
build/
dist/
*.egg-info/
*.egg

.venv/
venv/
ENV/
.env

.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

.idea/
.DS_Store

# Character and world saves must never end up in git
*.plr
*.plr.bak
*.wld
*.wld.bak

# The built web client IS committed (see scripts/bundle-web.py) — it is what makes a
# zero-install run serve the full UI instead of a bare JSON API.
